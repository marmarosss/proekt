import 'package:flutter/material.dart';

// глоьальна функція для рожевого жирного текстц
TextStyle pinkBoldTextStyle(double fontSize) {
  return TextStyle(
    fontSize: fontSize,
    fontWeight: FontWeight.bold,
    color: Colors.pink,
  );
}